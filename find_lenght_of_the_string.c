#include<stdio.h>
#include<string.h>

void main()
{
    char str[100];
    int i;
    printf("enter the string : " );
    gets(str);
    for(i=0;i<=str[i];i++){

    //strlen(str);
    }
    printf(" length of the string = %d  ",strlen(str));
}
